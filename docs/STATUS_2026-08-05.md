# 2026-08-05 工具输入、上下游与结果显示收尾状态

## 本轮完成

- 建立 18 类工具统一检查矩阵，覆盖输入来源、本工具 ROI、结果 ROI、Fixture、结果通道和界面显示。
- 统一面积 ROI 的校验、联合 domain mask 和坐标映射；未绑定 ROI 仍执行整图，点/线 ROI 不再误当像素搜索区域。
- OCR 扩边、多点找色和检测类结果统一使用同一坐标域；结果输出按区域、检测框、线段、文本和测量通道筛选。
- 结果 ROI/Fixture 只允许兼容的前置结果；稳定 `toolId` 无效时不再静默回退到错误索引；工具链无效模式、自依赖、禁用上游、重复 ID 和无效通道会在执行前报告。
- 主图、工具卡片、任务总览和任务详情统一使用 `ToolResult`；NG/Fail 仍有几何结果时保留叠加，跳过结果才隐藏；Fixture 结果序号改为描述性下拉框。
- 发布脚本修复无 F: 盘时探测华睿运行库会中断打包的问题。

## 验证结果

| 项目 | 结果 |
| --- | --- |
| Release x64 主工程 | 构建成功，`x64/Release/Windows_imgui.exe` |
| Release 回归工程 | 构建成功，`RegressionTests.exe` |
| 完整回归测试 | `regression_tests: all tests passed` |
| diff 检查 | `git diff --check` 通过 |
| Markdown 围栏检查 | 全部配对 |
| 运行包 | `dist/IMgui_Opencv-Release-x64.zip` 生成并通过脚本验证 |
| 运行包资源 | 字体、工具图标、案例配方、OCR 模型、OPC UA 文件均已纳入 |

## 尚需人工确认

自动化无法代替实际窗口截图。发布前仍建议按 [UI_SMOKE_CHECKLIST.md](../Test/UI_SMOKE_CHECKLIST.md) 检查 1280×720、1920×1080、深浅主题，以及 NG 结果叠加、OCR 混合输出、Fixture 无结果占位和窄窗口换行。
