# 2026-08-09 当前项目状态

> 本页是当前代码、构建和验收情况的总入口。此前的 `STATUS_2026-08-02.md`、`STATUS_2026-08-05.md` 等保留为历史快照，不代表本页之后的状态。

## 已验证

| 项目 | 当前结果 |
| --- | --- |
| Release 主程序 | `x64/Release/Windows_imgui.exe` 已生成；再次链接时若程序正在运行会触发 Windows 文件锁 |
| Release 回归工程 | `Test/x64/Release/RegressionTests.exe` 已生成 |
| 完整回归 | `regression_tests: all tests passed`，退出码 0，约 12.5 秒 |
| 差异检查 | `git diff --check` 通过 |
| 工具链 | 工具输入、ROI、上下游结果、Fixture、结果通道和统一输出已纳入自动回归 |
| 运行包资源 | 字体、工具图标、案例配方、OCR 和 OPC UA 相关文件已纳入打包规则 |

## 当前仍需完成

1. 关闭正在运行的主程序后重新构建 Release，并重新生成最新运行包。当前 `dist/IMgui_Opencv-Release-x64.zip` 的时间早于本次 EXE 验证，不能直接作为最终交付包。
2. 按 [UI_SMOKE_CHECKLIST.md](../Test/UI_SMOKE_CHECKLIST.md) 完成 1280×720、1920×1080、2560×1440、深浅主题和 DPI 验收，并保存正式截图基线。
3. 按 [STABILITY_TEST_PLAN.md](../Test/STABILITY_TEST_PLAN.md) 完成至少 8 小时稳定性测试；正式交付建议执行 24 小时，并保留 `samples.csv` 与 `summary.json`。
4. 在安装海康 MVS、华睿 iRAYPLE Runtime 和真实 PLC/OPC UA 服务器的环境中完成现场硬件验收。
5. 整理当前工作树中的源代码、文档和资源改动后分批提交；当前仍有大量未提交改动，不能把工作树直接当作正式版本。

## 非阻塞说明

- Debug 第三方库缺少对象级 PDB 的提示已通过 `/IGNORE:4099` 处理，不影响项目自身调试符号。
- 回归 OPC UA 服务只监听本机回环地址并使用匿名 `SecurityPolicyNone`，仅用于测试夹具，不代表生产安全配置。
- MQTT、加密 OPC UA、可拖拽流程编辑和插件系统仍属于路线图能力，不是当前自动回归失败项。

## 验收入口

- [构建说明](BUILD.md)
- [发布说明](RELEASE.md)
- [工具链统一检查](TOOL_CHAIN_AUDIT_2026-08-03.md)
- [UI 冒烟清单](../Test/UI_SMOKE_CHECKLIST.md)
- [稳定性测试方案](../Test/STABILITY_TEST_PLAN.md)
