# 2026-08-25 当前项目状态

> 本页是当前源码、构建、运行包和验收情况的统一入口。此前的 `STATUS_*.md` 均为历史快照，不代表本页之后的状态。

## 本轮完成

| 项目 | 当前结果 |
| --- | --- |
| 项目结构 | 应用入口、工作流、任务组、结果呈现、工具面板和硬件策略已拆分到独立模块，主工程和回归工程已同步 |
| 16 槽相机 | 槽位独立连接、重连、采集、触发和帧缓存；任务按绑定槽取帧，多槽执行等待本轮新帧且不切换用户预览 |
| 相机资源调度 | 槽位按任务按需激活；最多 4 个任务并行；共享模型推理段串行保护，避免共享 Session 并发异常 |
| 华睿内存 | SDK 回调边界复制帧数据，停止/断开/重连释放旧帧与待处理请求，避免第二台及后续相机累计引用厂商缓冲 |
| 多点找色 | 输入与参考图按只读方式共享；灰度/二值图按需创建；轴对齐矩形 ROI 跳过旋转提取；输入不被预处理修改 |
| 图像复制 | `ImageState`、任务输入切换和结果快照减少无条件整图复制，并通过写时复制保持任务图像隔离 |
| 耗时口径 | 工具卡片显示 `prepare + execute`；任务卡片汇总任务工具耗时；顶部显示整轮墙钟时间；YOLO 缓存命中仍包含整图哈希 |
| 启动诊断 | 正式包严格检查同目录核心程序和运行库；Visual Studio 的 `bootstrap` 开发布局自动从父目录启动主程序，不再误报整包缺失 |
| GitHub | 仓库当前尚无 Release 或版本标签；首次 `v1.0.0` 待当前源码提交、推送后创建 |

## 构建与回归

- `Windows_imgui.vcxproj` Release x64：成功，0 警告、0 错误。
- `Bootstrap/Bootstrap.vcxproj` Release x64：成功，0 警告、0 错误。
- `RegressionTests.exe --multicolor-only`：通过。
- `RegressionTests.exe --task-images-only`：通过。
- `RegressionTests.exe` 完整回归：通过。
- Visual Studio 开发布局：`--diagnostic-only` 退出码 0，能够启动父目录 `Windows_imgui.exe`。
- 正式发布包布局：`--diagnostic-only` 退出码 0，能够启动同目录 `Windows_imgui_core.exe`。

## 唯一正式运行包

- 目录：`dist/IMgui_Opencv-Release-x64/`
- ZIP：`dist/IMgui_Opencv-Release-x64.zip`
- 大小：`102,263,385` 字节（97.53 MiB）
- SHA256：`D66097E8E4DE249D22DC6EA4369E394990EE5514B3C17777817D28DFC722FA19`
- 脚本内容校验：通过，共包含启动器、核心程序、相机诊断、OpenCV 5、ONNX Runtime、DirectML、NCNN、MSVC Runtime、字体、图标、配方案例和模型。
- 当前打包机未发现海康 MVS Runtime 或华睿 MV Viewer Runtime，因此 ZIP 不携带厂商 SDK；目标机必须安装对应 x64 Runtime。

## 发布前仍需完成

1. 在真实 3 台及目标 16 台华睿相机环境验证连接、连续抓帧、断线重连、峰值工作集和长时间循环。
2. 完成 `Test/UI_SMOKE_CHECKLIST.md` 的正式截图基线与高 DPI 实机检查。
3. 完成稳定性浸泡，生成 `samples.csv` 和 `summary.json`。
4. 确认提交范围，将当前源码和文档提交并推送到 `master`。
5. 创建首次 GitHub `v1.0.0` Release，并上传唯一 ZIP；上传后核对远端大小和 SHA256。
