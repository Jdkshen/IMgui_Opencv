# Development Status: 2026-07-25

> 历史资料：本文保留 2026-07-25 合并时的英文记录和提交信息，不代表当前工作树；当前文档入口见 `docs/README.md`。


## GitHub update

- Merged `codex/p0-p4-release` into the GitHub default branch `master`.
- Pushed `master` to `origin/master`.
- Latest merge commit: `64ddcba Merge branch 'codex/p0-p4-release'`.
- The local `master` branch is synchronized with `origin/master` after the push.

## Main changes merged

- Refactored application components and updated the main UI workflows.
- Restored rotated ROI behavior and geometry drawing support.
- Added geometry primitives and the geometry draw editor.
- Added the run-result window for clearer inspection result review.
- Added preview texture caching for result and tool previews.
- Added frame archiving support for hardware/camera inspection flows.
- Added recipe autosave and persisted tool parameters.
- Persisted hardware connection settings.
- Linked camera capture to tool execution and trigger-before-inspection flows.
- Preserved and stopped pending camera inspection loops correctly.
- Added camera exposure controls.
- Added SPC database support and expanded inspection history/statistics behavior.
- Expanded tool execution graph, dependency handling, and tool settings coverage.
- Refined tool UI panels, grouping, labels, execution options, and batch operations.

## Code-to-documentation mapping

| Code area | Files | Documented behavior |
| --- | --- | --- |
| Geometry drawing | `Algorithm/GeometryDrawTool.*`, `UI/GeometryDrawEditor.*`, `Core/GeometryPrimitive.*`, `Core/RotatedROI.*` | Geometry primitives, rotated ROI restoration, draw-tool editing, and result overlays. |
| Results | `UI/RunResultWindow.*` | Dedicated run-result review window. |
| Tool settings/UI | `UI/Tools/BasicToolPanels.*`, `Core/ToolSettings.h`, `UI/ToolsWindow.cpp` | Shared tool panels, persisted settings, grouping and execution controls. |
| Execution graph | `Core/ToolExecutionGraph.*`, `Core/ToolController.*`, `Core/ToolExecutor.*` | Tool dependency handling, execution ordering, and input/output flow. |
| Persistence | `Core/RecipeAutosaveService.*`, `Core/HardwareSettingsService.*`, `Core/RecipeManager.*` | Recipe autosave, persisted tool parameters, and hardware settings. |
| Hardware/camera | `Core/HardwareRuntimeService.*`, `Core/OpenCvCameraAdapter.*`, `UI/HardwareWindow.cpp` | Camera exposure, trigger-before-inspection flow, pending-loop handling, and hardware outputs. |
| Traceability/SPC | `Core/FrameArchiveService.*`, `Core/SpcDatabase.*`, `Core/InspectionHistory.*`, `UI/StatsWindow.cpp` | Frame archive, inspection history, SPC storage, and statistics UI. |
| Preview rendering | `Renderer/PreviewTextureCache.*` | Cached preview textures for result/tool preview rendering. |
## Verification performed during merge

```text
git fetch origin
git pull --ff-only origin master
git merge --no-edit codex/p0-p4-release
git push origin master
git status --short --branch
```

The merge completed without conflicts. Final status showed `master` aligned with
`origin/master`.

## Notes

- No pull request was created because the branch was merged locally into `master`
  and pushed directly to GitHub.
- The feature branch `codex/p0-p4-release` still exists locally and on GitHub.
