# Claude Local Settings

This directory is reserved for local Claude/Codex workspace settings.

Do not commit `settings.local.json` or other machine-specific permission files. They can contain local paths and unsafe permission preferences.
